spring:
  devtools:
    restart:
      enabled: true

  datasource:
    url: jdbc:mysql://localhost:3306/stockmate
    username: root
    password: rlaqudcks715
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true
